import { TypeaheadMatchPipe } from './typeahead-match.pipe';

describe('TypeaheadMatchPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeaheadMatchPipe();
    expect(pipe).toBeTruthy();
  });
});
